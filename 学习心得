通过选择对应分支的code模块的codespaces，可以在云上创建一个基于Web的vscode平台
这个是一个虚拟平台，在这个VScode上可以对repository进行操作
类似于在本地仓库操作一样，对文件进行修改后在终端需要输入：
1. git add filename
2. git commit -m ""
3. git push
对了，在remote repository进行操作后可以 git pull,这样就不需要重新打开codespaces了。
